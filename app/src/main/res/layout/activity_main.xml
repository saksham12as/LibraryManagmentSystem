<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background">

    <!-- Main Background Image -->
    <ImageView
        android:id="@+id/mainBackgroundImage"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        android:src="@drawable/background_main"
        android:contentDescription="Library Background Image"
        android:alpha="0.8" />

    <!-- Title Text -->
    <TextView
        android:id="@+id/welcomeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Library Management System"
        android:textSize="22sp"
        android:textColor="@android:color/white"
        android:fontFamily="sans-serif-medium"
        android:layout_below="@id/mainBackgroundImage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <!-- Buttons Section -->
    <LinearLayout
        android:id="@+id/buttonsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_below="@id/welcomeText"
        android:layout_marginTop="30dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp">

        <!-- Add Book Button -->
        <Button
            android:id="@+id/addBookButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add Book"
            android:drawableLeft="@drawable/ic_add_book"
            android:drawablePadding="12dp"
            android:padding="14dp"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/button_color"
            android:layout_marginBottom="12dp" />

        <!-- View Books Button -->
        <Button
            android:id="@+id/viewBooksButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="View Books"
            android:drawableLeft="@drawable/ic_view_books"
            android:drawablePadding="12dp"
            android:padding="14dp"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/button_color"
            android:layout_marginBottom="12dp" />

        <!-- Borrow Book Button -->
        <Button
            android:id="@+id/borrowBookButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Borrow Book"
            android:drawableLeft="@drawable/ic_borrow_book"
            android:drawablePadding="12dp"
            android:padding="14dp"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/button_color"
            android:layout_marginBottom="12dp" />

        <!-- Manage Borrowers Button -->
        <Button
            android:id="@+id/manageBorrowersButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Manage Borrowers"
            android:drawableLeft="@drawable/ic_manage_borrowers"
            android:drawablePadding="12dp"
            android:padding="14dp"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/button_color"
            android:layout_marginBottom="12dp" />

    </LinearLayout>

</RelativeLayout>
